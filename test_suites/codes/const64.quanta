// Regression test: 64-bit integer literals beyond 32-bit range must compile
// and print correctly (no 64-bit-const crash). Expect 5000000000.
fn main() {
  let big = 5000000000
  printi(big)
  println(0)
  return 0
}
