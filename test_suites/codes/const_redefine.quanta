// const redefinition is rejected — the second const x = 10 must NOT overwrite the first.
// Before 0.0.75 const x = 5; const x = 10; silently returned 10 (second value won).
// After 0.0.75 the duplicate is skipped and x stays 5.
const x = 5
const x = 10
fn main() { return x }   // 5
