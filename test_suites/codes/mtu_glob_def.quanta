// cross-TU DATA global: DEFINING TU (entry, has _start)
global SHARED = 5

// add_shared is defined in the other TU (mtu_glob_use) and linked in.
extern "C" fn add_shared(d);

fn main() {
    let x = add_shared(20)   // SHARED = 5 + 20 = 25
    return x
}
