// Array push into a type-annotated empty array literal, then read back.
// This is the idiom the generic map<T,U> example uses; the IR_APUSH /
// IR_CLOSURE opcode collision made it silently yield 0.
fn main() {
    let r = [U]()
    r.push(3)
    r.push(4)
    return r[0] + r[1]     // 7
}
