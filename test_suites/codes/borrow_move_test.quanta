// Borrow checker test - move semantics
fn main() {
    let x = 42
    let y = move x
    // x should be moved, using it should fail borrow check
    // println(x)  // This should cause borrow check error
    return y
}