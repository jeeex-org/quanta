enum Color { Red, Green, Blue }
fn main() {
  let x = 5
  let c = Color::Green
  let a = match x { 1 => 10, 2 => 20, _ => 30 }
  let b = match c {
    Color::Red => 1,
    Color::Green => 2,
    Color::Blue => 3
  }
  let n = match x { 5 => 100, _ => 0 }
  exit(a + b + n)
}
