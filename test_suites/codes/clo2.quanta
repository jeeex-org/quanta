fn capture_mutate_shared() {
    let total = 10
    let add = |x| { total = total + x; return total }
    add(5)   // total 15
    add(7)   // total 22
    return total
}
fn main() { return capture_mutate_shared() }
