// 0.0.63: user enums — explicit variant tags via match.
enum Pri { Low=1, Mid=5, High=9 }
fn main() {
  let p = Pri.High
  let r = match p { Pri.Low => 10, Pri.Mid => 20, Pri.High => 30 }
  return r                // 30
}
