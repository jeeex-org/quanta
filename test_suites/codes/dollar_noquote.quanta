// $$(cmd) no-quote form: $$ followed by space captures the rest of the line
// as a raw command string. Equivalent to $$("ls -l /").
fn main() {
    unsafe {
        let r = $$ ls -l ~/
        return r.code
    }
}
