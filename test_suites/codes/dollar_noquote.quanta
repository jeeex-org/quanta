// $$(cmd) no-quote form: $$ followed by space captures the rest of the line
// as a raw command string. Equivalent to $$("/bin/echo hi").
fn main() {
    unsafe {
        let r = $$ /bin/echo hi
        return r.code
    }
}
