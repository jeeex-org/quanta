// $$(cmd) string form: $$("echo hi") runs an external command via fork/execve.
// Verifies the result struct's .code field is 0 (success).
fn main() {
    unsafe {
        let r = $$("/bin/echo hi")
        return r.code
    }
}
