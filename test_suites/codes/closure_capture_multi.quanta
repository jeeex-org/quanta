// P11 closure captures: multiple distinct captures, and a capture referenced
// more than once must reuse a single env slot (not duplicate it).
fn main() {
    let a = 3
    let b = 4
    let f = |x| { x + a + b + a }
    return f(1)        // 1 + 3 + 4 + 3 = 11
}
