fn main() -> i64 {
    // file_open now remaps the path string (header+8) internally.
    // Pass the path directly; expect a valid fd (>= 3) for an existing file.
    let fd = file_open("test_suites/codes/simple.quanta", 0)
    if fd < 3 { return 100 }
    return 11
}
