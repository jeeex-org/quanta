fn main() {
    // getenv is a STUB in 0.0.55: it returns 0 unconditionally
    // (environment parsing not yet implemented). This test pins that
    // behaviour so a future real implementation is caught by the gate.
    return getenv("HOME")
}
