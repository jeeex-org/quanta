// P5.1 multi-TU test fixture: the CALLER translation unit.
// Declares square() from mtu_helper.quanta as extern "C" and returns its result
// (7*7 = 49). Linked against mtu_helper.o to verify cross-TU call resolution.
extern "C" fn square(x);
fn main() {
  let n = 7;
  return square(n)
}
