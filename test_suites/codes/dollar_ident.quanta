// $$(identifier) shorthand — a single bare identifier inside $$() is stringified.
// $$(ls) == $$("ls"). The result's .code field is 0 (success).
fn main() {
    unsafe {
        let r = $$(ls)
        return r.code
    }
}
