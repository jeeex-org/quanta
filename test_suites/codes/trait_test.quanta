trait Drawable {
    fn draw(self) -> int
}

struct Circle {
    radius: int
}

impl Drawable for Circle {
    fn draw(self) -> int {
        self.radius * 2
    }
}

fn draw_shape(d: Drawable) {
    d.draw()
}

fn main() {
    let c = Circle { radius: 5 }
    draw_shape(c)
    return 0
}