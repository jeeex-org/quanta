// alias_test: `alias newname = existingfn` function alias (scan_aliases).
// Verifies an aliased function call resolves to the original.
fn helper() { return 42 }
alias h2 = helper
fn main() {
  if h2() != 42 { return 1 }
  return 0
}
