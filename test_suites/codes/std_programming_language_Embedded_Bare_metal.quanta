module std_programming_language_Embedded_Bare_metal;

import std/ir
import std/types
import std/patterns

// Domain: programming_language, Category: Embedded, Module: Bare-metal

fn init() -> i64 {
    return 0;
}

fn main() -> i64 {
    return init();
}
