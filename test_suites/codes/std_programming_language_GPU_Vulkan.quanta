module std_programming_language_GPU_Vulkan;

import std/ir
import std/types
import std/patterns

// Domain: programming_language, Category: GPU, Module: Vulkan

fn init() -> i64 {
    return 0;
}

fn main() -> i64 {
    return init();
}
