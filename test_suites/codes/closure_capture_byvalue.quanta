// P11 closure captures are BY VALUE, snapshotted at construction time.
// Mutating the captured local afterwards must NOT change what the closure sees.
fn main() {
    let y = 10
    let f = |x| { x + y }
    y = 99
    return f(1)        // 11, NOT 100
}
