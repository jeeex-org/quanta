module std_programming_language_Embedded_RTOS;

import std/ir
import std/types
import std/patterns

// Domain: programming_language, Category: Embedded, Module: RTOS

fn init() -> i64 {
    return 0;
}

fn main() -> i64 {
    return init();
}
