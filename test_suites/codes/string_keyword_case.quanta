// 0.0.78: String keyword accepts any case (simplicity). 'string' == 'String'.
// NOTE: String type itself is still todo (0.1.0); this test only checks the
// keyword is *recognized* (not 'undeclared variable') when spelled lowercase.
fn main() {
    let x: string = 0
    return x   // 0
}
