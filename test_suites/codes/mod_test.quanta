// 0.0.64: modules — `mod Name { fn ... }` + `Mod.fn()` qualified calls.
mod Math {
  fn add(a, b) { return a + b }
  fn mul(a, b) { return a * b }
}

fn main() {
  let x = Math.add(3, 4)     // 7
  let y = Math.mul(5, 6)     // 30
  let z = Math.add(x, y)     // 37
  return z
}