// P5.1 multi-TU test fixture: the CALLEE translation unit.
// Emitted with --no-start so it carries no _start/__init entry and its
// symbols do not collide with a second TU's globals.
fn square(x) {
  return x * x
}
