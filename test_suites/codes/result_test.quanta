fn divide(a, b) {
    if b == 0 {
        return Err("division by zero")
    }
    return Ok(a / b)
}

fn main() {
    let result = divide(10, 2)
    match result {
        Ok(value) => value,
        Err(e) => 0,
    }
}