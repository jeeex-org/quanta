fn increment_counter() {
    let count = 0
    let inc = |x| { count = count + x; return count }
    let a = inc(1)   // 1
    let b = inc(1)   // 2
    return a * 100 + b
}
fn main() { return increment_counter() }
