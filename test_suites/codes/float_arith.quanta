fn main() {
  # fadd/fmul do float math internally but return the truncated integer.
  let s = fadd(i2f(2), i2f(3))   # 2.0 + 3.0 = 5
  let p = fmul(s, i2f(4))         # 5 * 4.0 = 20
  exit(p)                         # 20
}
