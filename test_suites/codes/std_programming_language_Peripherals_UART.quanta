module std_programming_language_Peripherals_UART;

import std/ir
import std/types
import std/patterns

// Domain: programming_language, Category: Peripherals, Module: UART

fn init() -> i64 {
    return 0;
}

fn main() -> i64 {
    return init();
}
