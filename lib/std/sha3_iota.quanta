// lib/std/sha3_iota.quanta - Keccak iota step (FIPS 202)

include "sha3_constants.quanta"

fn keccak_iota(st, rnd) {
  let rc = keccak_rc(rnd)
  let a0 = mem_load(st + 0) ^ rc
  mem_store(st + 0, a0)
}