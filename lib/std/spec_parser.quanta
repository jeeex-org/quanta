// std/spec_parser.quanta — Phase 1 Gap Detection: parse docs/roadmap/*.md + domains/*.md
// Produces gap_specs.json (array of {domain, module, status, version, notes, in_domains})

module std_spec_parser;

const GAPS_CAP = 10000;

fn main() {
    return 0;
}
